{
  "name": "seanmh.com",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "predev": "node scripts/write-git-log.mjs",
    "prebuild": "node scripts/write-git-log.mjs",
    "dev": "astro dev",
    "build": "astro build",
    "check": "astro check && tsc --noEmit",
    "preview": "npm run build && wrangler pages dev dist",
    "astro": "astro",
    "deploy": "npm run build && wrangler pages deploy dist --project-name seanmh-terminal"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@chenglou/pretext": "^0.0.5",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.3",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3",
    "wrangler": "^4.83.0"
  }
}
